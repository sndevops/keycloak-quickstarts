apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: keycloak
spec:
  tls:
    - hosts:
      - 10.0.0.232
  rules:
  - host: 10.0.0.232
    http:
      paths:
      - backend:
          serviceName: keycloak
          servicePort: 8080

